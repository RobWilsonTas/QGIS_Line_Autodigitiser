This is a processing script for QGIS that allows the user to select a raster, a colour and a threshold

Then the script will construct lines where it finds that colour

There is also a polygon version
